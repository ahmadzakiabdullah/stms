# Users API (Future)

> **Status: Not implemented.** STMS currently exposes authenticated Laravel/Inertia web routes only. This file reserves the future /api/v1 contract area and does not describe an available production endpoint.

Design this contract only when the deferred REST API milestone is approved. Until then, use the corresponding web workflow documented in CURRENT_STATE.md.
